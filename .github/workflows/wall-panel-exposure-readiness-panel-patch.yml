name: Wall Panel Exposure Readiness Patch

on:
  push:
    branches:
      - lum-rpe-takeover
    paths:
      - .github/workflows/wall-panel-exposure-readiness-panel-patch.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch-wall-readiness-panel:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout canonical branch
        uses: actions/checkout@v4
        with:
          ref: lum-rpe-takeover

      - name: Apply inspectable wall readiness patch
        run: python scripts/patch-wall-panel-exposure-readiness-panel.py

      - name: Commit readiness panel wiring if changed
        run: |
          git config user.name "RPE Wall Readiness Patch Carrier"
          git config user.email "actions@users.noreply.github.com"
          git add src/components/Viewport3D.tsx
          if git diff --cached --quiet; then
            echo "Wall readiness panel already wired."
            exit 0
          fi
          git diff --cached --check
          git commit -m "Wire wall exposure readiness review into Phase 4"
          git push origin HEAD:lum-rpe-takeover
